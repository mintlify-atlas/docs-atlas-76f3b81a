---
title: Custom Edges
description: Build custom edge components with labels, buttons, and interactive elements
---

This example shows how to create custom edge components in Vue Flow. You'll learn how to add interactive elements to edges, use custom labels, and create different edge types.

## Live Demo

View the [live demo](https://vueflow.dev/examples/#/edges) on vueflow.dev.

## Complete Example

<CodeGroup>
```vue EdgesExample.vue
<script lang="ts" setup>
import { VueFlow } from '@vue-flow/core'
import { Background } from '@vue-flow/background'
import { Controls } from '@vue-flow/controls'
import { MiniMap } from '@vue-flow/minimap'
import CustomEdge from './CustomEdge.vue'
import CustomEdge2 from './CustomEdge2.vue'
import { initialEdges, initialNodes } from './initial-elements'
</script>

<template>
  <VueFlow :edges="initialEdges" :nodes="initialNodes" fit-view-on-init snap-to-grid>
    <template #edge-custom="props">
      <CustomEdge v-bind="props" />
    </template>

    <template #edge-custom2="props">
      <CustomEdge2 v-bind="props" />
    </template>

    <MiniMap />
    <Controls />
    <Background />
  </VueFlow>
</template>
```

```vue CustomEdge.vue
<script lang="ts" setup>
import type { EdgeProps, Position } from '@vue-flow/core'
import { EdgeLabelRenderer, getBezierPath, useVueFlow } from '@vue-flow/core'
import type { CSSProperties } from 'vue'

interface CustomEdgeProps<T = any> extends EdgeProps<T> {
  id: string
  sourceX: number
  sourceY: number
  targetX: number
  targetY: number
  sourcePosition: Position
  targetPosition: Position
  data: T
  markerEnd: string
  style?: CSSProperties
}

const props = defineProps<CustomEdgeProps>()

const { removeEdges } = useVueFlow()

const path = computed(() => getBezierPath(props))
</script>

<script lang="ts">
export default {
  inheritAttrs: false,
}
</script>

<template>
  <path :id="id" :style="style" class="vue-flow__edge-path" :d="path[0]" :marker-end="markerEnd" />

  <EdgeLabelRenderer>
    <div
      :style="{
        pointerEvents: 'all',
        position: 'absolute',
        transform: `translate(-50%, -50%) translate(${path[1]}px,${path[2]}px)`,
      }"
      class="nodrag nopan"
    >
      <button class="edgebutton" @click="removeEdges(id)">×</button>
    </div>
  </EdgeLabelRenderer>
</template>

<style>
.edgebutton {
  border-radius: 999px;
  cursor: pointer;
}
.edgebutton:hover {
  box-shadow: 0 0 0 2px pink, 0 0 0 4px #f05f75;
}
</style>
```

```typescript initial-elements.ts
import type { Edge, Node } from '@vue-flow/core'
import { MarkerType } from '@vue-flow/core'
import CustomLabel from './CustomLabel.vue'

export const initialNodes: Node[] = [
  { id: '1', type: 'input', label: 'Input 1', position: { x: 250, y: 0 } },
  { id: '2', label: 'Node 2', position: { x: 150, y: 100 } },
  { id: '3', label: 'Node 3', position: { x: 250, y: 200 } },
  { id: '4', label: 'Node 4', position: { x: 400, y: 300 } },
  { id: '5', label: 'Node 5', position: { x: 250, y: 400 } },
  { id: '6', type: 'output', label: 'Output 6', position: { x: 50, y: 550 } },
  { id: '7', type: 'output', label: 'Output 7', position: { x: 250, y: 550 } },
]

export const initialEdges: Edge[] = [
  { 
    id: 'e1-2', 
    source: '1', 
    target: '2', 
    label: 'bezier edge (default)', 
    class: 'normal-edge' 
  },
  { 
    id: 'e2-3', 
    source: '2', 
    target: '3', 
    type: 'step', 
    label: 'step edge' 
  },
  { 
    id: 'e3-4', 
    source: '3', 
    target: '4', 
    type: 'straight', 
    label: 'straight edge' 
  },
  { 
    id: 'e4-5', 
    source: '4', 
    target: '5', 
    animated: true, 
    label: 'animated edge', 
    style: { stroke: 'red' } 
  },
  {
    id: 'e5-6',
    source: '5',
    target: '6',
    label: h(CustomLabel, { label: 'custom label' }, {}),
    labelStyle: { fill: 'red', fontWeight: 700 },
    markerEnd: {
      type: MarkerType.Arrow,
    },
  },
  {
    id: 'e5-7',
    source: '5',
    target: '7',
    type: 'custom',
    data: { text: 'custom edge' },
    markerEnd: {
      type: MarkerType.ArrowClosed,
    },
  },
]
```
</CodeGroup>

## Key Concepts

### Registering Custom Edges

Use the `#edge-{type}` template slot to register custom edge components:

```vue
<VueFlow :edges="edges" :nodes="nodes">
  <template #edge-custom="props">
    <CustomEdge v-bind="props" />
  </template>
</VueFlow>
```

The slot name must match the `type` property of your edge.

### Edge Props

Custom edge components receive these props:

```typescript
interface EdgeProps<T = any> {
  id: string
  type: string
  source: string
  target: string
  sourceX: number
  sourceY: number
  targetX: number
  targetY: number
  sourcePosition: Position
  targetPosition: Position
  data?: T
  markerStart?: string
  markerEnd?: string
  style?: CSSProperties
  label?: string | Component
  // ... and more
}
```

## Built-in Edge Types

Vue Flow provides several built-in edge types:

<Tabs>
  <Tab title="Bezier (Default)">
    Smooth curved edge (default type):
    
    ```typescript
    { id: 'e1', source: '1', target: '2' }
    // or explicitly
    { id: 'e1', source: '1', target: '2', type: 'default' }
    ```
  </Tab>
  
  <Tab title="Straight">
    Direct straight line:
    
    ```typescript
    { id: 'e1', source: '1', target: '2', type: 'straight' }
    ```
  </Tab>
  
  <Tab title="Step">
    Step edge with right angles:
    
    ```typescript
    { id: 'e1', source: '1', target: '2', type: 'step' }
    ```
  </Tab>
  
  <Tab title="Smooth Step">
    Step edge with rounded corners:
    
    ```typescript
    { id: 'e1', source: '1', target: '2', type: 'smoothstep' }
    ```
  </Tab>
</Tabs>

## Creating Custom Edges

### Basic Custom Edge

A custom edge needs to render the path and optionally labels:

```vue
<script setup>
import { getBezierPath } from '@vue-flow/core'

const props = defineProps<EdgeProps>()

const path = computed(() => getBezierPath(props))
</script>

<template>
  <path 
    :id="id" 
    :style="style" 
    class="vue-flow__edge-path" 
    :d="path[0]" 
    :marker-end="markerEnd" 
  />
</template>
```

### Path Helpers

Vue Flow provides helper functions for different path types:

```typescript
import { 
  getBezierPath,
  getStraightPath,
  getStepPath,
  getSmoothStepPath 
} from '@vue-flow/core'

// Returns [pathString, labelX, labelY, offsetX, offsetY]
const [path, labelX, labelY] = getBezierPath({
  sourceX: props.sourceX,
  sourceY: props.sourceY,
  targetX: props.targetX,
  targetY: props.targetY,
  sourcePosition: props.sourcePosition,
  targetPosition: props.targetPosition,
})
```

### Adding Interactive Elements

Use `EdgeLabelRenderer` to add interactive elements to edges:

```vue
<template>
  <path :d="path[0]" class="vue-flow__edge-path" />
  
  <EdgeLabelRenderer>
    <div
      :style="{
        position: 'absolute',
        transform: `translate(-50%, -50%) translate(${path[1]}px,${path[2]}px)`,
        pointerEvents: 'all',
      }"
      class="nodrag nopan"
    >
      <button @click="removeEdges(id)">Delete</button>
    </div>
  </EdgeLabelRenderer>
</template>
```

<Note>
  The `nodrag` and `nopan` classes prevent the edge button from triggering drag or pan events.
</Note>

## Edge Styling

### Inline Styles

```typescript
const edges = ref([
  {
    id: 'e1',
    source: '1',
    target: '2',
    style: { 
      stroke: 'red',
      strokeWidth: 2 
    }
  }
])
```

### Animated Edges

```typescript
const edges = ref([
  {
    id: 'e1',
    source: '1',
    target: '2',
    animated: true
  }
])
```

### Edge Labels

<Tabs>
  <Tab title="Simple Label">
    ```typescript
    {
      id: 'e1',
      source: '1',
      target: '2',
      label: 'Edge Label'
    }
    ```
  </Tab>
  
  <Tab title="Styled Label">
    ```typescript
    {
      id: 'e1',
      source: '1',
      target: '2',
      label: 'Styled Label',
      labelStyle: { fill: 'red', fontWeight: 700 },
      labelBgStyle: { fill: '#FFCC00', fillOpacity: 0.7 },
      labelBgPadding: [8, 4],
      labelBgBorderRadius: 4
    }
    ```
  </Tab>
  
  <Tab title="Component Label">
    ```typescript
    import CustomLabel from './CustomLabel.vue'
    
    {
      id: 'e1',
      source: '1',
      target: '2',
      label: h(CustomLabel, { text: 'Custom' }, {})
    }
    ```
  </Tab>
</Tabs>

## Markers

Add arrow markers to edges:

```typescript
import { MarkerType } from '@vue-flow/core'

const edges = ref([
  {
    id: 'e1',
    source: '1',
    target: '2',
    markerEnd: {
      type: MarkerType.ArrowClosed,
      width: 20,
      height: 20,
      color: '#FF0072'
    },
    markerStart: {
      type: MarkerType.Arrow
    }
  }
])
```

Available marker types:
- `MarkerType.Arrow`
- `MarkerType.ArrowClosed`

## Edge Data

Pass custom data to edges:

```typescript
const edges = ref([
  {
    id: 'e1',
    source: '1',
    target: '2',
    type: 'custom',
    data: {
      text: 'Custom data',
      onDelete: handleDelete
    }
  }
])
```

Access it in your component:

```vue
<script setup>
const props = defineProps<EdgeProps<{
  text: string
  onDelete: () => void
}>>()
</script>

<template>
  <div>{{ data.text }}</div>
  <button @click="data.onDelete">Delete</button>
</template>
```

## TypeScript Support

```typescript
import type { EdgeProps } from '@vue-flow/core'

interface CustomEdgeData {
  label: string
  count: number
}

interface CustomEdgeProps extends EdgeProps<CustomEdgeData> {
  data: CustomEdgeData
}

const props = defineProps<CustomEdgeProps>()
```

## Best Practices

<Note>
  Always add `inheritAttrs: false` to custom edge components:
  
  ```typescript
  export default {
    inheritAttrs: false,
  }
  ```
</Note>

## Next Steps

<CardGroup cols={2}>
  <Card title="Custom Nodes" icon="cube" href="/examples/custom-nodes">
    Create custom node components
  </Card>
  
  <Card title="Validation" icon="check-circle" href="/examples/validation">
    Validate connections before creation
  </Card>
  
  <Card title="Edge API" icon="book" href="/api/types/edge">
    Complete edge type reference
  </Card>
  
  <Card title="Edge Label Renderer" icon="tag" href="/guides/custom-edges">
    EdgeLabelRenderer documentation
  </Card>
</CardGroup>
